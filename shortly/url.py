def lookup(url):
    """input: url
        - find id for url
        - return id or False
    """


def get(id):
    """input: id
        - find url for id
        - forward request to url
        - 404 if id is not found
    """


def make(url):
    """input: url
        - generate id
        - ensure uniqueness
        - store id & url in ddb
        - return short url
    """
